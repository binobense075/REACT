# Everything that react can do , we can do thatr using normal HTML,CSS & JS then why REACT?

- It makes the developer experience easy.
- It helps us to write less code and do more on the web page , this is the major job of UI library or framework.

- Never keep hardcoded data in the component folder.

# utils

- Utilities that can be used all across the world.

- Always name the constant variables in caps.

- can we use default import along with named import in same file?

  - the UI layer will respond based on the data layer.

- React is efficient/fast om DOM manipulation.

- Suppose if you want to keep the data and the ui layer tied/comsitent to each other, that is where react / or any library comes in to the play.(Updating the DOM Effiecienty)

# Why react is fast?

- Because it cam do faster DOM Manipulations.

# State Variable

- It is like the state of the component.
- To create a state variable, we use REACT HOOKS, which is known as USE STATE.

# React Hooks

- It is a normal JS function given by react but it has some superpowers
- Normal JS utility functons (When we import react we got all the utility functions of it)
- import the reacthooks as a named import
- Why use state is called state varaible? Because it maintains the state of the component.
- If you create a state varible inside a componnent , it;s scope is only inside that component.
- How to create a state variable?
- When you call the useState, it will give a state variable we recieve the state variable inside an array.
- How to modify a state variable?
- - useState() - Used to create state varibales
- useEffect()

- Whenever a state variable updates react re-renders the component.

- React will keep in sync with the UI layer iwth the data layer

-- react doesn't touch the actual dom, it finds Virtual DOM tree and update the Actual DOM
