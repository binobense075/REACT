# Higher Order Components

- Higher Order Function is a function that takes the component and returns a component, and the component is enhanced version of input component.
- It takes a component and enhances/modifies it and return the component.
- Higher order commponents are pure functions,

- Important part of react application is to manage it's data.
- UI (is static) is different part and data is different part inside React Application
- React applciations has two layer, UI layer and Data Layer, and the Ui layer is powered by the data layer.
- UI layer is JSX
- Data layer is state, props, local variable, {} braces

# Now see how to manage data properly in our app

# Controlled and Uncontrolled Components

# Lifting the State Up

- If the parent has full control over the children then it is controlled component, if not uncontrolled component.
- Lifiting State Up - sometimes we want to lift the state up if we want to control the children.

# Props Drilling

- React is a one way data flow
- When the application is huge, passing data between components is complex.
- React has one way data flow.
- We want to pass the data through nested children, this concept is known as props drilling.

# useContext

- Contect solves the problem of props drilling, it is like a central store to store data.
-
